services:
  app:
    build: .
    volumes:
      - .:/var/www/html
    ports:
      - "80:80"
    environment:
      - APP_NAME=ProttashaAcademic
      - CI_ENVIRONMENT=production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
